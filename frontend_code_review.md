# 前端代码视觉规范审查报告

基于 `frontend/UI_STYLE_GUIDE.md` 对当前项目前端代码进行审查。

审查范围：
- `frontend/app.html`
- `frontend/assets/styles.css`
- `frontend/assets/app.js`
- `web/ai-config/index.html`

---

## 一、严重问题（Must Fix）

### 1.1 颜色变量不符合规范

**问题位置**: `frontend/assets/styles.css:3-14`

| 规范要求 | 实际代码 | 状态 |
|---------|---------|------|
| `--text: #1F2937` | `--text-title: #111827` | ❌ |
| `--muted: #6B7280` | `--muted: #9ca3af` | ❌ |
| `--danger: #DC2626` | `--danger: #f97316` | ❌ 实际是橙色 |
| `--success: #16A34A` | `--success: #16a34a` | ⚠️ 大小写不一致 |

**建议修复**:
```css
:root {
  --primary: #2563eb;
  --primary-soft: #dbeafe;
  --primary-gradient: linear-gradient(135deg, #2563eb, #1d4ed8);
  --success: #16A34A;
  --warning: #F97316;
  --danger: #DC2626;
  --bg: #F5F7FB;
  --card: #FFFFFF;
  --border: #E5E7EB;
  --text: #1F2937;
  --muted: #6B7280;
}
```

### 1.2 CSS类名与HTML不匹配

**问题**: HTML使用 `.btn-secondary`，CSS定义 `.btn--secondary`

**位置**:
- HTML: `frontend/assets/app.js:364,365`
- CSS: `frontend/assets/styles.css:164`

**影响**: 次要按钮样式无法应用

**修复方案**: 统一使用 BEM 命名规范 `btn--secondary`

### 1.3 JavaScript引用的CSS类未定义

**问题**: `app.js` 中使用了大量未在 `styles.css` 中定义的类

**缺失类名**:
```javascript
// app.js 使用但 styles.css 未定义:
.question-header          // 行331
.question-type-badge      // 行331
.question-progress        // 行332
.question-prompt          // 行335
.options-list            // 行340
.option-item             // 行344
.option-label            // 行345
.feedback-box            // 行368
.correct                 // 行559
.incorrect               // 行559
.feedback-title          // 行562
.btn-next                // 行581
.btn-submit              // 行366
.option-disabled         // 行615
```

**影响**: 这些元素没有样式或样式不符合规范

---

## 二、重要问题（Should Fix）

### 2.1 字号不符合规范

| 元素 | 规范要求 | 实际代码 | 位置 |
|-----|---------|---------|------|
| H2 | 24px / 20px (移动) | 16px | `styles.css:91` |
| H3 | 18px / 16px (移动) | 20px | `styles.css:322` |

### 2.2 表单控件规格不符合

**输入框高度**:
- 规范: `44px`
- 实际: `padding: 10px 12px` (动态高度)
- 位置: `styles.css:338-347`

**圆角**:
- 规范: `12px`
- 实际: `10px`
- 位置: `styles.css:343`

**建议修复**:
```css
.form-input,
.form-select {
  height: 44px;
  padding: 0 12px;
  border-radius: 12px;
  /* ... */
}
```

### 2.3 标签（Chip/Tag）内边距不符合

- 规范: `padding: 4px 10px`
- 实际: `padding: 6px 12px`
- 位置: `styles.css:417`

### 2.4 页面内边距不使用响应式单位

**问题**: `.main-panel` 使用固定值 `32px 48px`

**规范要求**:
```css
.main-panel {
  padding: clamp(24px, 5vw, 48px);
}

@media (max-width: 959px) {
  .main-panel {
    padding: 16px;
  }
}
```

**当前代码**: `styles.css:220-225`

### 2.5 缺少 `.btn--danger` 样式

**规范要求**: 危险按钮样式（浅红底 + 红字）

**建议添加**:
```css
.btn--danger {
  background: #FEE2E2;
  color: var(--danger);
  border: 1px solid var(--danger);
}

.btn--danger:hover {
  background: #FCA5A5;
}
```

### 2.6 反馈组件缺少颜色区分

**规范**: 正确使用浅绿色背景 + 绿色边框；错误使用浅红色背景 + 红色边框

**当前代码**: `.feedback` 只有基础样式，缺少 `.feedback--correct` 和 `.feedback--incorrect`

**建议添加**:
```css
.feedback--correct {
  background: #D1FAE5;
  border: 2px solid var(--success);
}

.feedback--incorrect {
  background: #FEE2E2;
  border: 2px solid var(--danger);
}
```

---

## 三、建议改进（Nice to Have）

### 3.1 卡片头部缺少分隔样式

**规范**: `border-bottom: 1px solid #EEF2FF`

**建议**: 为 `.question-card__header` 添加底部边框

### 3.2 缺少公共样式文件

**规范建议**: 将公共变量与混入放在 `frontend/assets/variables.css`

**当前**: 所有样式混在 `styles.css` 中

**建议结构**:
```
frontend/assets/
├── variables.css    # 颜色、字号、间距变量
├── base.css         # 重置样式、基础排版
├── components.css   # 按钮、卡片、表单组件
└── styles.css       # 页面特定样式
```

### 3.3 动效时长不一致

**规范**: `0.2s ~ 0.3s`，使用 `ease-in-out`

**当前代码**: 部分使用 `0.2s ease`，部分使用 `0.3s`

**建议**: 统一为 `transition: all 0.2s ease-in-out`

### 3.4 选项禁用状态不符合规范

**规范**: `opacity: 0.6; pointer-events: none`

**当前代码**: `app.js:615-616` 使用 `.option-disabled` 和内联样式

**建议**: 在CSS中定义规范样式

---

## 四、AI配置页面审查

**文件**: `web/ai-config/index.html`

### 4.1 缺少样式文件审查

未提供 `web/ai-config/styles.css`，无法审查是否符合规范。

### 4.2 HTML结构问题

- 使用了自定义类名 `.panel`, `.field`, `.actions` 等
- 需确保这些类遵循主规范的颜色、字号、间距标准

---

## 五、HTML语义化问题

### 5.1 缺少必要的ARIA属性

**建议添加**:
```html
<!-- 上传区域 -->
<div class="upload-zone" role="button" tabindex="0" aria-label="点击或拖拽上传文件">

<!-- 题目选项 -->
<div class="option" role="button" tabindex="0" aria-pressed="false">

<!-- 反馈框 -->
<div class="feedback-box" role="alert" aria-live="polite">
```

### 5.2 按钮类型缺失

**问题**: 多个按钮未指定 `type="button"`

**位置**: `app.js` 动态生成的按钮

**修复**: 所有非提交按钮应添加 `type="button"`

---

## 六、优先级修复清单

### P0 - 立即修复（影响功能）

1. ✅ 修复 `.btn-secondary` vs `.btn--secondary` 类名不匹配
2. ✅ 为 `app.js` 引用的所有类添加CSS定义
3. ✅ 修正颜色变量（特别是 `--danger` 和 `--text`）

### P1 - 高优先级（影响用户体验）

4. ✅ 修复表单输入框高度为 `44px`，圆角为 `12px`
5. ✅ 修正 H2、H3 字号
6. ✅ 添加 `.feedback--correct` 和 `.feedback--incorrect` 样式
7. ✅ 添加 `.btn--danger` 样式

### P2 - 中优先级（规范一致性）

8. ⚠️ 修复标签内边距为 `4px 10px`
9. ⚠️ 页面内边距改用 `clamp()` 响应式单位
10. ⚠️ 统一动效时长为 `0.2s ease-in-out`

### P3 - 低优先级（代码质量）

11. 📋 重构样式文件结构（拆分 variables、components、base）
12. 📋 添加ARIA无障碍属性
13. 📋 补充卡片头部分隔线样式

---

## 七、代码规范建议

### 7.1 命名规范

**当前**: 混用 BEM 和普通命名
```css
.btn--primary     /* BEM */
.question-card    /* 普通 */
.feedback-box     /* 普通 */
```

**建议**: 统一使用 BEM 命名
```css
.btn--primary
.question__card
.feedback__box
```

### 7.2 CSS变量使用

**当前**: 部分地方硬编码颜色
```css
/* styles.css:823 */
background: #fee2e2;  /* 应使用变量 */
```

**建议**: 所有颜色通过变量引用
```css
background: var(--danger-light);
```

### 7.3 代码组织

**建议顺序**:
1. 位置属性 (position, top, left)
2. 盒模型 (display, width, height, padding, margin)
3. 边框 (border, border-radius)
4. 背景 (background, box-shadow)
5. 文本 (font, color, text-align)
6. 其他 (cursor, transition)

---

## 八、总结

### 符合规范的部分 ✅

- 主色调、背景色基本正确
- 字体族完全符合
- 响应式断点设置合理
- 按钮圆角（10px → 应改为12px）
- 卡片圆角（18px）和阴影
- 选项组件的间距和圆角

### 需要修复的部分 ❌

- **严重**: 10个CSS类未定义
- **严重**: 颜色变量不符合规范
- **重要**: 字号不符合规范（H2、H3）
- **重要**: 表单控件规格不符
- **重要**: 缺少必要的组件样式（danger按钮、feedback状态）

### 预计工作量

- P0问题修复: **2-3小时**
- P1问题修复: **1-2小时**
- P2+P3改进: **3-4小时**
- **总计**: 6-9小时

---

## 九、附录：完整修复示例

详见下一份文档 `frontend_fix_plan.md`
