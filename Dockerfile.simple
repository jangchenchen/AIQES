# 简化版 Dockerfile - 用于快速验证
# 跳过系统依赖安装，使用纯 Python 实现

FROM python:3.11-slim

LABEL maintainer="qa-system"
LABEL description="QA System - Simplified build for testing"

# 创建非 root 用户
RUN useradd -m -u 1000 -s /bin/bash qauser

WORKDIR /app

# 复制应用代码
COPY src/ ./src/
COPY web_server.py ./
COPY requirements-web.txt ./

# 安装 Python 依赖（跳过需要编译的包）
RUN pip install --no-cache-dir Flask==3.0.3 openai anthropic && \
    chown -R qauser:qauser /app

USER qauser

# 创建数据目录
RUN mkdir -p data uploads docs/Knowledge AI_cf

# 暴露端口
EXPOSE 5001

# 健康检查
HEALTHCHECK --interval=30s --timeout=5s --start-period=5s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:5001/').read()"

# 启动命令
CMD ["python", "web_server.py"]
